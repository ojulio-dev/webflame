próximo passo do chat:

componente sidebar: Conseguir pegar todos os usuários que eu sigo e que já me mandaram uma mensagem.

componente sidebar: trazer pelo created_at.

componente sidebar: fazer o script da troca de selecionado.

componente message-content: conseguir pegar os dados do usuário clicado.

componente message-content: conseguir pegar as mensagens do usuário clicado.

componente message-content: conseguir enviar uma mensagem para o usuário clicado.

Depois de tudo isso: Conseguir fazer o "digitando" e a atualização automática da tela.